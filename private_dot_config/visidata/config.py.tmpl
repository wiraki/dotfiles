# Colors
options.color_default = "7 on 234"
options.color_menu = "16 on 68"

# Other
{{- if eq .chezmoi.hostname "workPC" }}
options.clipboard_copy_cmd = "xclip -selection clipboard"
{{- else }}
options.clipboard_copy_cmd = "wl-copy"
{{- end }}

# Default float formatting
options.disp_float_fmt = "{:.08f}"

# Custom keybinds
visidata.BaseSheet.bindkey("F3", "rename-sheet")
