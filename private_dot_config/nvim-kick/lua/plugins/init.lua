-- [[ Plugin Modules ]]
-- This file exports all plugin configurations for lazy.nvim to load
-- Each plugin category is organized in its own file for better maintainability

-- NOTE: Lazy.nvim will automatically load all lua files in the plugins/ directory
-- when using `{ import = "plugins" }` in the lazy.setup() configuration.
--
-- This init.lua file is optional but provided for documentation purposes.
-- The actual plugin loading happens through the import in config/lazy.lua

return {}
