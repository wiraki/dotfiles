#!/bin/sh

if [ ! -f "/home/marc/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/marc/.config/chezmoi"
    chezmoi age decrypt --output "/home/marc/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/marc/.config/chezmoi/key.txt"
fi
